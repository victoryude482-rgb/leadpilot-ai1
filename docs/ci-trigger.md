# LeadPilot CI

This file exists to trigger the repository validation workflow after the CI workflow was installed. The validation pipeline is the source of truth for typecheck, lint, tests, and production build status.
