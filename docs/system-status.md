# LeadPilot System Status

The main branch contains the integrated agent, communication, website/brand, orchestration, and CI changes. GitHub Actions is configured to validate every push to `main` with install, typecheck, lint, test, and production build steps.

This document is informational only; workflow results remain the authoritative build status.
