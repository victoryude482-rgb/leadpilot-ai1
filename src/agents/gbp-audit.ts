import type { BusinessRecord } from '../leads/model';
import type { LeadProvider } from '../providers/lead-provider';
import { runLeadFinderPipeline, type FinderPipelineResult } from '../pipeline/lead-finder-pipeline';
import { runPythonAgent } from './python-worker';
import type { AgentRunInput } from './runtime';
export type GbpAuditGrade='GOOD'|'NEEDS_WORK'|'POOR';export type GbpIssueCode='NO_WEBSITE'|'DEAD_WEBSITE'|'NO_PHONE'|'NO_ADDRESS'|'NO_CATEGORY';
export interface GbpIssue{code:GbpIssueCode;title:string;evidence:string;severity:'high'|'medium'|'low'}
export interface GbpAudit{leadId?:string;business:BusinessRecord;score:number;grade:GbpAuditGrade;issues:GbpIssue[];basis:'public-directory-inference'|'google-places';disclaimer:string;checkedAt:string;problem?:string;opportunity?:string;recommendation?:string}
async function websiteReachability(url?:string){if(!url)return{checked:false,reachable:false};const attempt=async(method:'HEAD'|'GET')=>{const controller=new AbortController();const timer=setTimeout(()=>controller.abort(),5000);try{return await fetch(url,{method,redirect:'follow',signal:controller.signal,cache:'no-store'})}finally{clearTimeout(timer)}};try{let response:Response;try{response=await attempt('HEAD')}catch{response=await attempt('GET')}return{checked:true,reachable:response.ok,detail:`${response.status} ${response.statusText}`} }catch(error){return{checked:true,reachable:false,detail:error instanceof Error?error.message:'Website request failed'}}}
async function googlePlacesCheck(business:BusinessRecord):Promise<Partial<GbpAudit>|null>{const key=process.env.GOOGLE_PLACES_API_KEY?.trim();if(!key)return null;const text=[business.name,business.address,business.city,business.country].filter(Boolean).join(', ');if(!text)return null;try{const search=await fetch('https://places.googleapis.com/v1/places:searchText',{method:'POST',headers:{'content-type':'application/json','X-Goog-Api-Key':key,'X-Goog-FieldMask':'places.id,places.displayName'},body:JSON.stringify({textQuery:text,maxResultCount:1}),cache:'no-store'});if(!search.ok)return null;const data=await search.json() as {places?:Array<{id?:string}>};const place=data.places?.[0];if(!place?.id)return null;const details=await fetch(`https://places.googleapis.com/v1/places/${encodeURIComponent(place.id)}`,{headers:{'X-Goog-Api-Key':key,'X-Goog-FieldMask':'id,displayName,formattedAddress,nationalPhoneNumber,primaryType'},cache:'no-store'});if(!details.ok)return null;const actual=await details.json() as {formattedAddress?:string;nationalPhoneNumber?:string;primaryType?:string};const issues:GbpIssue[]=[];if(!actual.formattedAddress)issues.push({code:'NO_ADDRESS',title:'No address returned by Google Places',evidence:'The Google Places record did not include a formatted address.',severity:'medium'});if(!actual.nationalPhoneNumber)issues.push({code:'NO_PHONE',title:'No phone returned by Google Places',evidence:'The Google Places record did not include a national phone number.',severity:'medium'});if(!actual.primaryType)issues.push({code:'NO_CATEGORY',title:'No primary category returned by Google Places',evidence:'The Google Places record did not include a primary type.',severity:'medium'});return{score:Math.max(0,100-issues.length*15),grade:issues.length>=3?'POOR':issues.length?'NEEDS_WORK':'GOOD',issues,basis:'google-places',disclaimer:'This audit used the optional Google Places Text Search + Place Details result for the searched business. It does not claim that every field in the live Google Business Profile is complete.'}}catch{return null}}
export async function auditBusiness(business:BusinessRecord):Promise<GbpAudit>{const places=await googlePlacesCheck(business);if(places)return{business,score:places.score??100,grade:places.grade??'GOOD',issues:places.issues??[],basis:'google-places',disclaimer:places.disclaimer??'',checkedAt:new Date().toISOString()};const issues:GbpIssue[]=[];if(!business.website)issues.push({code:'NO_WEBSITE',title:'No public website listed',evidence:'No website was present in the public directory data used by LeadPilot.',severity:'high'});else{const reachability=await websiteReachability(business.website);if(reachability.checked&&!reachability.reachable)issues.push({code:'DEAD_WEBSITE',title:'Website was not reachable',evidence:`The public website check did not receive a successful response (${reachability.detail??'unknown error'}).`,severity:'high'})}if(!business.phone)issues.push({code:'NO_PHONE',title:'No public phone listed',evidence:'No phone number was present in the public directory data used by LeadPilot.',severity:'medium'});if(!business.address&&!business.city)issues.push({code:'NO_ADDRESS',title:'No public address listed',evidence:'No street address or city was present in the public directory data used by LeadPilot.',severity:'medium'});if(!business.industry)issues.push({code:'NO_CATEGORY',title:'No category/industry listed',evidence:'No category or industry was available in the public directory record.',severity:'low'});const deductions=issues.reduce((sum,issue)=>sum+(issue.code==='NO_WEBSITE'||issue.code==='DEAD_WEBSITE'?30:issue.code==='NO_PHONE'||issue.code==='NO_ADDRESS'?15:10),0);const score=Math.max(0,100-deductions);return{business,score,grade:score>=80?'GOOD':score>=55?'NEEDS_WORK':'POOR',issues,basis:'public-directory-inference',disclaimer:'This is an inferred listing-health audit from public directory data and a live website reachability check. It is not a direct read of the business\'s real Google Business Profile. Configure GOOGLE_PLACES_API_KEY for the optional Google Places check.',checkedAt:new Date().toISOString()}}
export async function runGbpAudit(accountId:string,providers:LeadProvider[],query:{keywords?:string;industry?:string;country?:string;city?:string;limit?:number}):Promise<{audits:GbpAudit[];warnings:string[];strategy?:string[]}>{const result=await runLeadFinderPipeline(accountId,providers,query);const audits=(await Promise.all(result.results.map(async(item:FinderPipelineResult)=>{const audit=await auditBusiness(item.business);return {...audit,leadId:item.lead.id}}))).filter(audit=>audit.issues.length>0);const python=await runPythonAgent({agent:'gbp-audit',query:query.keywords||'GBP audit',location:query.city,industry:query.industry,country:query.country,limit:query.limit} as AgentRunInput,{businesses:audits.map(a=>a.business),audits});if(!python)return{audits,warnings:result.warnings,strategy:['TypeScript performs lead discovery, Places/API checks and control-plane data handling.','Python reasoning was unavailable; TypeScript audit remains the safe fallback.']};const enriched=audits.map((audit,index)=>{const reasoning=python.results[index] as Record<string,unknown>|undefined;return{...audit,problem:typeof reasoning?.problem==='string'?reasoning.problem:undefined,opportunity:typeof reasoning?.opportunity==='string'?reasoning.opportunity:undefined,recommendation:typeof reasoning?.recommendation==='string'?reasoning.recommendation:undefined}});return{audits:enriched,warnings:result.warnings,strategy:['TypeScript owns discovery, Google Places/API checks and control-plane data.','Python owns evidence interpretation, problem identification, opportunity reasoning and recommendations.']}}
