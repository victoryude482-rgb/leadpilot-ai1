'use client';

import { useState } from 'react';

type Lead={name?:string;company?:string;industry?:string;location?:string;website?:string;email?:string;painPoint?:string;evidence?:string};
type Result={score:number;tier:'hot'|'warm'|'cold';reasons:string[];outreach:{subject:string;body:string;followUp:string;personalization:string}};

export default function PipelinePage(){
 const [token,setToken]=useState('');const [offer,setOffer]=useState('');const [lead,setLead]=useState<Lead>({});const [result,setResult]=useState<Result|null>(null);const [loading,setLoading]=useState(false);const [error,setError]=useState('');
 async function generate(){setLoading(true);setError('');setResult(null);try{const r=await fetch('/api/agents/pipeline',{method:'POST',headers:{'content-type':'application/json',...(token?{authorization:`Bearer ${token}`}:{})},body:JSON.stringify({offer,lead})});const d=await r.json();if(!r.ok)throw new Error(d.error||'Could not build sales package.');setResult(d)}catch(e){setError(e instanceof Error?e.message:'Could not build sales package.')}finally{setLoading(false)}}
 const set=(k:keyof Lead)=>(e:React.ChangeEvent<HTMLInputElement>)=>setLead(v=>({...v,[k]:e.target.value}));
 return <main className="page"><header><div><p>VICTORY AI · SALES PIPELINE</p><h1>Turn a lead into an outreach-ready opportunity.</h1></div><input className="token" value={token} onChange={e=>setToken(e.target.value)} placeholder="Session token"/></header><section className="card"><h2>Lead information</h2><div className="fields">{(['name','company','industry','location','website','email','painPoint','evidence'] as (keyof Lead)[]).map(k=><label key={k}>{k}<input value={lead[k]||''} onChange={set(k)} placeholder={k==='evidence'?'e.g. outdated website, hiring, new expansion':''}/></label>)}</div><label>What are you selling?<input value={offer} onChange={e=>setOffer(e.target.value)} placeholder="e.g. website redesign and local SEO"/></label><button onClick={generate} disabled={loading||!offer}>{loading?'Building…':'Qualify + create outreach →'}</button>{error&&<div className="error">{error}</div>}</section>{result&&<section className="result"><div className="score"><span>{result.tier.toUpperCase()}</span><strong>{result.score}</strong><small>/100 qualification score</small></div><div><h2>{result.outreach.personalization}</h2><h3>Why this score</h3><ul>{result.reasons.map((x,i)=><li key={i}>{x}</li>)}</ul></div><div className="draft"><h3>{result.outreach.subject}</h3><pre>{result.outreach.body}</pre><h3>Follow-up</h3><pre>{result.outreach.followUp}</pre></div><p className="note">Draft only — Victory AI does not send this message automatically.</p></section>}<style jsx>{`:global(body){margin:0;background:#07101b;color:#eaf2f8;font-family:Inter,system-ui,sans-serif}.page{max-width:1000px;margin:auto;padding:28px}.page header{display:flex;justify-content:space-between;gap:20px;align-items:end;padding:30px 0}.page header p{font-size:10px;letter-spacing:.15em;color:#6dd8b5;font-weight:800}.page h1{font-size:clamp(34px,6vw,62px);line-height:1;letter-spacing:-.04em;max-width:760px;margin:0}.card,.result{background:#0b1723;border:1px solid #203243;border-radius:18px;padding:24px;margin-top:20px}.fields{display:grid;grid-template-columns:repeat(2,1fr);gap:12px;margin-bottom:14px}label{display:flex;flex-direction:column;gap:7px;color:#8fa3b5;font-size:10px;text-transform:uppercase;letter-spacing:.08em;margin-bottom:14px}input{height:44px;border:1px solid #2a3d4f;border-radius:9px;background:#07111c;color:#eef5fa;padding:0 12px;font-size:14px}.token{width:180px}.card button{height:45px;border:0;border-radius:9px;background:#7ee7c4;color:#06110f;font-weight:800;padding:0 18px;cursor:pointer}.card button:disabled{opacity:.5}.error{margin-top:14px;color:#ffb8bd}.result{display:grid;grid-template-columns:180px 1fr;gap:25px}.score{display:flex;flex-direction:column;align-items:center;justify-content:center;border:1px solid #315748;border-radius:14px;padding:20px}.score span{font-size:11px;color:#7ee7c4;letter-spacing:.15em}.score strong{font-size:54px}.score small{color:#70879a}.result h2{margin-top:0}.result li{color:#a7b8c5;margin:7px 0}.draft{grid-column:1/-1;border-top:1px solid #203243;padding-top:20px}.draft pre{white-space:pre-wrap;background:#07111c;border:1px solid #203243;border-radius:10px;padding:16px;color:#c7d5df;line-height:1.55;font-family:inherit}.note{grid-column:1/-1;color:#71889a;font-size:11px}@media(max-width:650px){.page{padding:16px}.page header{display:block}.token{margin-top:18px;width:100%}.fields{grid-template-columns:1fr}.result{grid-template-columns:1fr}.draft{grid-column:auto}.note{grid-column:auto}}`}</style></main>
}
