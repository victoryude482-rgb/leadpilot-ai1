'use client';

import { FormEvent, useState } from 'react';
import { searchLeadFinder, searchTrendLeads } from '../../src/api/lead-finder-client';
import { researchQuestion, type ResearchResponse } from '../../src/api/research-client';
import LeadQualityBadge from '../../components/lead-quality-badge';

type Lead = { lead?: { id?: string; score?: number; scoreLabel?: string; status?: string }; business?: { name?: string; website?: string; phone?: string; email?: string; city?: string; country?: string; industry?: string }; report?: { confidence?: number; level?: string; recommendation?: string } };
type Trend = { title: string; source: string; url?: string; community?: string; relevance: number };
type SearchResult = { results?: Lead[]; count?: number; warnings?: string[]; error?: string; trends?: Trend[]; strategy?: string[] };
type SmartResearch = ResearchResponse & { insights?: Array<{ type: string; text: string; confidence: number }>; followUpQueries?: string[]; sources?: Array<{ title: string; url: string; source: string; snippet?: string; publishedAt?: string; evidenceScore?: number; evidenceType?: string }> };

function interpretPrompt(prompt: string) { const text = prompt.trim(); const lower = text.toLowerCase(); const countryNames = ['nigeria','ghana','kenya','south africa','united states','usa','canada','uk','united kingdom','india','australia','germany','france','uae','united arab emirates']; const country = countryNames.find(x => lower.includes(x)) ?? ''; const city = country === 'nigeria' ? (['lagos','abuja','port harcourt','ibadan','benin city','kano'].find(x => lower.includes(x)) ?? '') : ''; const cleaned = text.replace(/\b(in|near|around|at)\s+(lagos|abuja|port harcourt|ibadan|benin city|kano|nigeria|ghana|kenya|canada|india|australia|germany|france|uk|usa)\b/gi, ' ').replace(/\s+/g,' ').trim(); return { industry: '', country, city, keywords: cleaned || text }; }

export default function LeadFinderPage() { const [prompt,setPrompt]=useState(''); const [filters,setFilters]=useState({industry:'',country:'',city:'',keywords:'',limit:25}); const [trendMode,setTrendMode]=useState(true); const [state,setState]=useState<'idle'|'loading'|'done'|'error'>('idle'); const [result,setResult]=useState<SearchResult>({}); const [research,setResearch]=useState<SmartResearch>({}); const [researching,setResearching]=useState(false);
  async function submit(event:FormEvent){event.preventDefault();const parsed=interpretPrompt(prompt||filters.keywords);const next={...filters,...parsed};setFilters(next);setState('loading');try{const body=trendMode?await searchTrendLeads(next):await searchLeadFinder(next);setResult(body as SearchResult);setState('done');}catch(error){setResult({error:error instanceof Error?error.message:'Search failed'});setState('error');}}
  async function researchNow(event:FormEvent){event.preventDefault();if(!prompt.trim())return;setResearching(true);setResearch({});try{setResearch(await researchQuestion(prompt) as SmartResearch);}catch(error){setResearch({error:error instanceof Error?error.message:'Research failed'});}finally{setResearching(false);}}
  return <main style={{maxWidth:960,margin:'0 auto',padding:32,fontFamily:'system-ui'}}><h1>LeadPilot AI</h1><p>Describe what you want in normal language. The system researches the request, searches multiple sources, finds real businesses, verifies evidence and ranks opportunities.</p><section style={{marginTop:24,padding:18,border:'1px solid #ddd',borderRadius:14}}><h2>Smart AI Search</h2><p style={{opacity:.7}}>One prompt. Research + trends + lead discovery.</p><form onSubmit={submit} style={{display:'grid',gap:10}}><input value={prompt} onChange={e=>setPrompt(e.target.value)} placeholder="Try: AI automation opportunities for restaurants in Lagos" style={{padding:12,fontSize:16}}/><div style={{display:'flex',gap:8}}><button disabled={state==='loading'} type="submit">{state==='loading'?'Researching & finding…':'Search anything'}</button><button type="button" disabled={researching||!prompt.trim()} onClick={researchNow}>{researching?'Researching…':'Deep research'}</button></div></form>{research.error&&<p role="alert">{research.error}</p>}{research.answer&&<div style={{marginTop:16,padding:14,background:'#f6f6f6',borderRadius:10}}><strong>AI synthesis</strong><p>{research.answer}</p></div>}{research.insights?.length?<section style={{marginTop:16}}><h3>Evidence insights</h3>{research.insights.map((x,i)=><article key={i} style={{padding:10,border:'1px solid #eee',borderRadius:9,marginTop:8}}><strong>{x.type.toUpperCase()} · {x.confidence}%</strong><p>{x.text}</p></article>)}</section>:null}{research.sources?.length?<div style={{marginTop:16}}><h3>Ranked sources</h3>{research.sources.map((s,i)=>{const score='evidenceScore' in s&&typeof s.evidenceScore==='number'?s.evidenceScore:0;return <article key={`${s.url}-${i}`} style={{padding:'10px 0',borderBottom:'1px solid #eee'}}><a href={s.url} target="_blank" rel="noreferrer"><strong>{i+1}. {s.title}</strong></a><div style={{fontSize:12,opacity:.7}}>{s.source} · Evidence {score}/100</div>{s.snippet&&<div style={{fontSize:13}}>{s.snippet.slice(0,300)}</div>}</article>;})}</div>:null}</section><section style={{marginTop:28}}><h2>Lead Finder</h2><form onSubmit={submit} style={{display:'grid',gap:10,marginTop:12}}><details><summary>Advanced filters</summary><div style={{display:'grid',gap:8,marginTop:8}}>{(['industry','country','city','keywords'] as const).map(key=><input key={key} value={filters[key]} placeholder={key} onChange={e=>setFilters({...filters,[key]:e.target.value})}/>)}</div></details><label><input type="checkbox" checked={trendMode} onChange={e=>setTrendMode(e.target.checked)}/> Trend-first (Reddit + News + business discovery)</label></form><div style={{display:'flex',gap:8,flexWrap:'wrap',marginTop:16}}><LeadQualityBadge score={85}/><LeadQualityBadge score={55}/><LeadQualityBadge score={25}/><span style={{opacity:.7,alignSelf:'center'}}>Good · Medium · Bad</span></div>{result.error&&<p role="alert">{result.error}</p>}{state==='done'&&<p>{result.count??result.results?.length??0} leads found.</p>}{trendMode&&result.trends?.length?<section style={{marginTop:20,padding:16,border:'1px solid #ddd',borderRadius:12}}><h2>Trending signals</h2>{result.trends.map((trend,index)=><div key={`${trend.source}-${index}`} style={{padding:'10px 0',borderBottom:'1px solid #eee'}}><strong>{trend.title}</strong><div style={{fontSize:13,opacity:.75}}>{trend.source}{trend.community?` · ${trend.community}`:''} · relevance {trend.relevance}/100</div>{trend.url&&<a href={trend.url} target="_blank" rel="noreferrer">View signal</a>}</div>)}</section>:null}<section style={{display:'grid',gap:16,marginTop:16}}>{(result.results??[]).map((item,index)=><article key={item.lead?.id??index} style={{border:'1px solid #ddd',borderRadius:12,padding:16}}><div style={{display:'flex',justifyContent:'space-between',gap:12,alignItems:'center'}}><h2>{item.business?.name??'Unknown business'}</h2><LeadQualityBadge score={item.lead?.score}/></div><p>{item.business?.industry??'Industry unknown'} · {item.business?.city??''} {item.business?.country??''}</p><p>Score: <strong>{item.lead?.score??0}</strong> ({item.lead?.scoreLabel??'UNRATED'})</p><p>Reliability: {item.report?.level??'Pending'} {item.report?.confidence!=null?`(${item.report.confidence}%)`:''}</p><p>Recommendation: {item.report?.recommendation??'Review'}</p>{item.business?.website&&<a href={item.business.website} target="_blank" rel="noreferrer">Website</a>}</article>)}</section>{result.strategy?.length?<p>Strategy: {result.strategy.join(' · ')}</p>:null}{result.warnings?.length?<p>Provider warnings: {result.warnings.join(' · ')}</p>:null}</section></main>;
}
