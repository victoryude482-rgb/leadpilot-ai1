'use client';import { FormEvent,useState } from 'react';import { searchLeadFinder,searchTrendLeads } from '../../src/api/lead-finder-client';import { researchQuestion,type ResearchResponse } from '../../src/api/research-client';import LeadQualityBadge from '../../components/lead-quality-badge';import BusinessLink from '../../components/business-link';import SendDraftButton from '../../components/send-draft-button';
type Lead={lead?:{id?:string;score?:number;scoreLabel?:string};business?:{name?:string;website?:string;phone?:string;email?:string;address?:string;city?:string;country?:string;industry?:string;source?:string};report?:{confidence?:number;level?:string;recommendation?:string}};type Trend={title:string;source:string;url?:string;community?:string;relevance:number};type SearchResult={results?:Lead[];count?:number;warnings?:string[];error?:string;trends?:Trend[];strategy?:string[]};type SmartResearch=ResearchResponse;
export default function LeadFinderPage(){const[prompt,setPrompt]=useState('');const[filters,setFilters]=useState({industry:'',country:'',city:'',keywords:'',limit:25});const[trendMode,setTrendMode]=useState(false);const[result,setResult]=useState<SearchResult>({});const[research,setResearch]=useState<SmartResearch>({});const[loading,setLoading]=useState(false);const[researching,setResearching]=useState(false);const[token,setToken]=useState('');async function submit(e:FormEvent){e.preventDefault();setLoading(true);try{const body=trendMode?await searchTrendLeads(filters):await searchLeadFinder(filters);setResult(body as SearchResult)}catch(error){setResult({error:error instanceof Error?'Search failed: '+error.message:'Search failed'})}finally{setLoading(false)}}async function researchNow(){if(!prompt.trim())return;setResearching(true);try{setResearch(await researchQuestion(prompt) as SmartResearch)}catch(error){setResearch({error:error instanceof Error?error.message:'Research failed'})}finally{setResearching(false)}}return <main className="shell"><header><h1>LeadPilot AI · Lead Finder</h1><input value={token} onChange={e=>setToken(e.target.value)} placeholder="Signed-in session token (for approval send)"/></header><p>Find source-backed businesses. Trend signals are research evidence, not business leads.</p><section className="panel"><form onSubmit={submit}><input value={prompt} onChange={e=>{setPrompt(e.target.value);setFilters(f=>({...f,keywords:e.target.value}))}} placeholder="Salons in Lagos that need websites"/><input value={filters.city} onChange={e=>setFilters({...filters,city:e.target.value})} placeholder="City"/><input value={filters.industry} onChange={e=>setFilters({...filters,industry:e.target.value})} placeholder="Industry"/><button disabled={loading}>{loading?'Searching…':'Search businesses'}</button><button type="button" disabled={researching} onClick={researchNow}>{researching?'Researching…':'Deep research'}</button></form><label><input type="checkbox" checked={trendMode} onChange={e=>setTrendMode(e.target.checked)}/> Include trending discussion/news evidence</label>{research.error&&<p className="error">{research.error}</p>}{research.answer&&<div className="research"><b>AI synthesis</b><p>{research.answer}</p></div>}</section><section className="panel"><div className="legend"><LeadQualityBadge score={85}/><LeadQualityBadge score={55}/><LeadQualityBadge score={25}/> Good · Medium · Bad</div>{result.error&&<p className="error">{result.error}</p>}{result.warnings?.length?<div className="warnings"><b>Search agent notes</b>{result.warnings.map((warning,i)=><p key={i}>{warning}</p>)}</div>:null}{trendMode&&result.trends?.length?<section className="trends"><h2>Trending signals — discussion/news evidence, not businesses</h2>{result.trends.map((t,i)=><article key={i}><b>{t.title}</b><p>{t.source} · relevance {t.relevance}/100</p>{t.url&&<a href={t.url} target="_blank" rel="noreferrer">View signal</a>}</article>)}</section>:null}<h2>{result.count??result.results?.length??0} business leads</h2><div className="leads">{(result.results??[]).map((item,i)=>{const b=item.business??{};const id=item.lead?.id;return <article key={id??i}><div className="row"><h3>{b.name??'Unknown business'}</h3><LeadQualityBadge score={item.lead?.score}/></div><p>{b.industry??'Industry not listed'} · {[b.city,b.country].filter(Boolean).join(', ')||'Location not listed'}</p><BusinessLink name={b.name??'business'} address={b.address} city={b.city} country={b.country} website={b.website}/>{token&&id&&b.email?<SendDraftButton token={token} leadId={id} defaultTo={b.email} defaultSubject={`Quick idea for ${b.name??'your business'}`} defaultText={`Hi,\n\nI found ${b.name??'your business'} while researching businesses in your area. I have a short idea that may be useful.\n\nWould you be open to taking a look?\n\nBest,\nLeadPilot AI`}/>:null}</article>})}</div></section><style jsx>{`:global(body){margin:0;background:#07101b;color:#eaf2f8;font-family:Inter,system-ui,sans-serif}.shell{max-width:1100px;margin:auto;padding:24px}header{display:flex;justify-content:space-between;gap:15px;align-items:center}header input{max-width:360px}.panel{background:#0b1723;border:1px solid #203243;border-radius:15px;padding:18px;margin:20px 0}.panel form{display:grid;grid-template-columns:2fr 1fr 1fr auto auto;gap:8px}.panel input{height:42px;box-sizing:border-box;border:1px solid #2a3e50;border-radius:8px;background:#07111c;color:#fff;padding:0 10px}.panel button{border:0;border-radius:8px;background:#7ee7c4;font-weight:800}.legend{display:flex;gap:8px;align-items:center;margin-bottom:14px}.warnings{margin:0 0 14px;padding:12px;border:1px solid #6b5a31;border-radius:9px;background:#241e11}.warnings p{margin:5px 0;color:#d6c58c;font-size:12px}.leads{display:grid;grid-template-columns:repeat(2,1fr);gap:10px}.leads article,.trends article{background:#0f202b;border:1px solid #24394a;border-radius:11px;padding:14px}.row{display:flex;justify-content:space-between}.leads p,.trends p{color:#8da2b3;font-size:12px}.leads a,.trends a{color:#7ee7c4}.error{color:#ffb8bd}.research{padding:12px;background:#10231f;border-radius:9px}.trends{margin:20px 0;padding:14px;border:1px solid #6b5a31;border-radius:12px}@media(max-width:800px){.panel form,.leads{grid-template-columns:1fr}header{display:block}header input{max-width:none;width:100%;margin-top:10px}}`}</style></main>}
