'use client';import { FormEvent,useEffect,useState } from 'react';import AuthBar from '@/components/auth-bar';import BusinessLink from '@/components/business-link';import SendDraftButton from '@/components/send-draft-button';type Agent={id:string;name:string;description:string;status:string;capabilities:string[]};type Result={business?:{name?:string;website?:string;phone?:string;email?:string;address?:string;city?:string;country?:string;industry?:string};lead?:{id?:string;score?:number;scoreLabel?:string};businessName?:string;email?:string;subject?:string;body?:string;followUp?:string;issues?:string[];eligible?:boolean;eligibilityReason?:string};export default function Home(){const[agents,setAgents]=useState<Agent[]>([]);const[agent,setAgent]=useState('lead-finder');const[query,setQuery]=useState('');const[location,setLocation]=useState('');const[industry,setIndustry]=useState('');const[token,setToken]=useState('');const[loading,setLoading]=useState(false);const[error,setError]=useState('');const[results,setResults]=useState<Result[]>([]);const[fixWebsite,setFixWebsite]=useState('');const[fixPhone,setFixPhone]=useState('');const[dealClosed,setDealClosed]=useState(false);const[fixResult,setFixResult]=useState<unknown>(null);useEffect(()=>{fetch('/api/agents').then(r=>r.json()).then(d=>setAgents(d.agents??[])).catch(()=>{})},[]);async function run(e:FormEvent){e.preventDefault();setLoading(true);setError('');setResults([]);setFixResult(null);try{const headers:Record<string,string>={'content-type':'application/json'};if(token)headers.authorization=`Bearer ${token}`;const body={agent,query,location,industry,limit:25,...(agent==='gbp-fix'?{website:fixWebsite,phone:fixPhone,dealStatus:dealClosed?'CUSTOMER':'NEW'}:{})};const r=await fetch('/api/agents/run',{method:'POST',headers,body:JSON.stringify(body)});const d=await r.json();if(!r.ok)throw new Error(d.error??'Agent failed');if(agent==='gbp-fix')setFixResult(d);else setResults(d.results??d.audits??d.drafts??[])}catch(x){setError(x instanceof Error?x.message:'Agent failed')}finally{setLoading(false)}}const selected=agents.find(a=>a.id===agent);return <main className="shell"><header><div className="brand">LP <b>LeadPilot AI</b></div><AuthBar onTokenChange={setToken}/></header><section className="hero"><p>VICTORY'S WORKSPACE</p><h1>Run your AI business team.</h1><span>Discover leads, audit listings, find work, plan campaigns and turn closed deals into concrete delivery plans.</span></section><section><h2>Agents</h2><div className="agents">{agents.map(a=><button key={a.id} onClick={()=>setAgent(a.id)} className={a.id===agent?'selected':''}><strong>{a.name}</strong><small>{a.status}</small><p>{a.description}</p></button>)}</div></section><section className="panel"><h2>{selected?.name??'Agent'}</h2><p>{selected?.description}</p><form onSubmit={run}><input value={query} onChange={e=>setQuery(e.target.value)} placeholder={agent==='gbp-fix'?'Client/business name':'What do you want to find?'} required/><input value={location} onChange={e=>setLocation(e.target.value)} placeholder="City or country"/><input value={industry} onChange={e=>setIndustry(e.target.value)} placeholder="Industry"/>{agent==='gbp-fix'&&<><input value={fixWebsite} onChange={e=>setFixWebsite(e.target.value)} placeholder="Client website (optional)"/><input value={fixPhone} onChange={e=>setFixPhone(e.target.value)} placeholder="Client phone (optional)"/><label className="check"><input type="checkbox" checked={dealClosed} onChange={e=>setDealClosed(e.target.checked)}/> Deal closed with this client (CUSTOMER)</label></>}<button disabled={loading}>{loading?'Running…':'Run agent'}</button></form>{error&&<div className="error">{error}</div>}{fixResult&&<pre>{String(JSON.stringify(fixResult,null,2))}</pre>}</section><section><h2>Results</h2><div className="results">{results.map((item,i)=>{const b=item.business??{};const name=item.businessName??b.name??'Business';const email=item.email??b.email;const leadId=item.lead?.id;if(agent==='gbp-outreach'&&item.subject&&item.body)return <article key={leadId??i}><h3>{name}</h3><p>{item.issues?.join(' · ')}</p><p>{item.eligibilityReason}</p><pre>{item.body}</pre>{token&&leadId&&item.eligible&&email?<SendDraftButton token={token} leadId={leadId} defaultTo={email} defaultSubject={item.subject} defaultText={item.body}/>:null}</article>;return <article key={leadId??i}><div className="row"><h3>{name}</h3><b>{item.lead?.score??0}</b></div><p>{b.industry??industry??'Industry not listed'} · {[b.city,b.country].filter(Boolean).join(', ')||'Location not listed'}</p><BusinessLink name={name} address={b.address} city={b.city} country={b.country} website={b.website}/>{token&&leadId&&email?<SendDraftButton token={token} leadId={leadId} defaultTo={email} defaultSubject={`Quick idea for ${name}`} defaultText={`Hi,\n\nI found ${name} while researching businesses in your area. I have a short idea that may be useful for your business.\n\nWould you be open to taking a look?\n\nBest,\nLeadPilot AI`}/>:null}</article>})}</div>{!results.length&&<p className="muted">Results appear here after an agent run.</p>}</section><style jsx>{`:global(body){margin:0;background:#07101b;color:#eaf2f8;font-family:Inter,system-ui,sans-serif}.shell{max-width:1200px;margin:auto;padding:24px}header{display:flex;justify-content:space-between;align-items:center;border-bottom:1px solid #203243;padding-bottom:18px}.brand{font-size:20px;font-weight:800}.brand b{color:#7ee7c4}.hero{padding:60px 0 35px}.hero p{font-size:10px;letter-spacing:.15em;color:#7ee7c4}.hero h1{font-size:clamp(42px,7vw,72px);margin:0 0 12px}.hero span,.muted{color:#8da2b3}.agents{display:grid;grid-template-columns:repeat(4,1fr);gap:10px}.agents button,.panel,article{background:#0b1723;border:1px solid #203243;border-radius:14px;color:#eaf2f8;padding:16px;text-align:left}.agents button.selected{border-color:#64c7a9;background:#10231f}.agents small{display:block;color:#7ee7c4;margin-top:6px}.agents p{font-size:12px;color:#8da2b3;line-height:1.4}.panel{margin:25px 0}.panel form{display:grid;grid-template-columns:repeat(3,1fr);gap:10px}.panel input{height:44px;border:1px solid #2a3e50;border-radius:8px;background:#07111c;color:white;padding:0 12px}.panel button{height:44px;border:0;border-radius:8px;background:#7ee7c4;font-weight:800}.check{grid-column:1/-1;color:#a7b8c5;font-size:12px}.check input{height:auto}.error{margin-top:12px;padding:12px;background:#2a161a;border:1px solid #71343d;color:#ffb8bd;border-radius:8px}.results{display:grid;grid-template-columns:repeat(2,1fr);gap:12px}.results article{display:block}.results .row{display:flex;justify-content:space-between}.results a{display:inline-block;color:#7ee7c4;margin:7px 0}.results p{color:#8da2b3;font-size:12px}.results pre{white-space:pre-wrap;background:#07111c;padding:12px;border-radius:8px;line-height:1.5}@media(max-width:900px){.agents{grid-template-columns:repeat(2,1fr)}.panel form,.results{grid-template-columns:1fr}}@media(max-width:520px){.shell{padding:16px}.agents{grid-template-columns:1fr}}`}</style></main>}
