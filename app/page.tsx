'use client';

import { FormEvent, useEffect, useMemo, useState } from 'react';
import AuthBar from '@/components/auth-bar';

type Agent = { id:string; name:string; description:string; category:string; status:string; capabilities:string[] };
type Lead = { id?:string; business?:{name?:string;website?:string;industry?:string;location?:string;city?:string;country?:string;phone?:string;email?:string}; name?:string; company?:string; email?:string; website?:string; phone?:string; score?:number };
type RunResponse = { agent?:Agent; results?:Lead[]; warnings?:string[]; error?:string; message?:string; status?:string };

export default function Home() {
  const [query,setQuery]=useState(''); const [location,setLocation]=useState(''); const [industry,setIndustry]=useState(''); const [limit,setLimit]=useState('10');
  const [token,setToken]=useState(''); const [loading,setLoading]=useState(false); const [error,setError]=useState(''); const [message,setMessage]=useState(''); const [leads,setLeads]=useState<Lead[]>([]); const [warnings,setWarnings]=useState<string[]>([]);
  const [agents,setAgents]=useState<Agent[]>([]); const [selectedAgent,setSelectedAgent]=useState('lead-finder');
  useEffect(()=>{ fetch('/api/agents').then(r=>r.json()).then(d=>setAgents(d.agents||[])).catch(()=>{}); },[]);
  const visibleLeads=useMemo(()=>leads,[leads]);
  async function runSelectedAgent(event:FormEvent){ event.preventDefault(); setLoading(true); setError(''); setMessage(''); setWarnings([]); setLeads([]);
    try { const headers:Record<string,string>={'content-type':'application/json'}; if(token) headers.authorization=`Bearer ${token}`;
      const response=await fetch('/api/agents/run',{method:'POST',headers,body:JSON.stringify({agent:selectedAgent,query,location,industry,limit:Number(limit)})});
      const data=(await response.json()) as RunResponse; if(!response.ok) throw new Error(data.error||data.message||`Agent failed (${response.status})`);
      setLeads(data.results||[]); setWarnings(data.warnings||[]); setMessage(data.message||'Agent completed.');
      if(!(data.results||[]).length && !data.message) setError('The agent completed without results. Try broader input.');
    } catch(err){setError(err instanceof Error?err.message:'Unable to run the agent right now.');} finally{setLoading(false);}
  }
  const selected=agents.find(a=>a.id===selectedAgent);
  return <main className="shell"><header className="topbar"><div className="brand"><span className="logo">VA</span><span>Victory <b>AI</b></span></div><div className="topActions"><span className="badge">MULTI-AGENT BUSINESS INTELLIGENCE</span><AuthBar onTokenChange={setToken}/></div></header>
    <section className="hero"><div><p className="eyebrow">VICTORY'S WORKSPACE</p><h1>Find opportunities.<br/><span>Automate growth.</span></h1><p className="subtitle">One workspace for AI agents that discover leads, trends, opportunities and business signals.</p></div><div className="heroCard"><strong>{agents.length||8} AI agents</strong><span>research → score → monitor → act</span></div></section>
    <section className="agents"><div className="sectionTitle"><p className="eyebrow">AI AGENTS</p><h2>Choose an agent</h2></div><div className="agentGrid">{agents.map(a=><button type="button" key={a.id} className={`agentCard ${selectedAgent===a.id?'selected':''}`} onClick={()=>{setSelectedAgent(a.id);setError('');setMessage('');setLeads([])}}><div className="agentName"><strong>{a.name}</strong><span>{a.status}</span></div><p>{a.description}</p><small>{a.capabilities.slice(0,3).join(' · ')}</small></button>)}</div></section>
    <section className="panel"><div className="panelTitle"><div><h2>{selected?.name||'AI Lead Finder'}</h2><p>{selected?.description||'Tell the agent what you want to discover.'}</p></div><span className="live"><i/> {token?'SIGNED IN':'PUBLIC SEARCH'}</span></div><form onSubmit={runSelectedAgent} className="form"><label>What are you looking for<input value={query} onChange={e=>setQuery(e.target.value)} placeholder="e.g. businesses that need websites" required/></label><label>Location<input value={location} onChange={e=>setLocation(e.target.value)} placeholder="e.g. Lagos, London, Canada"/></label><label>Industry<input value={industry} onChange={e=>setIndustry(e.target.value)} placeholder="e.g. Technology"/></label><label>Results<select value={limit} onChange={e=>setLimit(e.target.value)}><option>5</option><option>10</option><option>25</option><option>50</option></select></label><button disabled={loading}>{loading?'Running…':'Run agent →'}</button></form>{message&&<div className="success"><strong>{selected?.name||'Agent'}</strong><span>{message}</span></div>}{error&&<div className="error"><strong>Agent needs attention</strong><span>{error}</span></div>}{warnings.length>0&&<div className="warning">{warnings.join(' · ')}</div>}</section>
    <section className="results"><div className="resultsHead"><div><p className="eyebrow">RESULTS</p><h2>{visibleLeads.length?`${visibleLeads.length} leads found`:'Your results will appear here'}</h2></div><div className="stats"><span><b>{visibleLeads.length}</b> FOUND</span><span><b>{visibleLeads.filter(l=>(l.score||0)>=70).length}</b> HIGH SCORE</span></div></div>{visibleLeads.length?<div className="grid">{visibleLeads.map((lead,i)=>{const company=lead.business?.name||lead.company||lead.name||'Unnamed business';const locationText=lead.business?.location||[lead.business?.city,lead.business?.country].filter(Boolean).join(', ')||location||'Location pending';const website=lead.website||lead.business?.website;return <article className="lead" key={lead.id||`${company}-${i}`}><div className="leadTop"><div className="avatar">{company.slice(0,1).toUpperCase()}</div><div><h3>{company}</h3><p>{lead.business?.industry||industry||'B2B prospect'}</p></div><strong className="score">{lead.score??'—'}</strong></div><div className="meta"><span>{locationText}</span><span>{lead.email||lead.business?.email||'Email not listed'}</span><span>{lead.phone||lead.business?.phone||'Phone not listed'}</span></div>{website&&<a href={website} target="_blank" rel="noreferrer">Visit website ↗</a>}</article>})}</div>:<div className="empty"><div className="emptyIcon">⌕</div><h3>{selectedAgent==='lead-finder'?'Run your first lead search':'Agent is connected'}</h3><p>{selectedAgent==='lead-finder'?'Start with a natural-language request and the multi-source lead engine will search legitimate data sources.':'This agent is registered in the runtime. Its dedicated evidence sources are being activated next; Victory AI will never invent results.'}</p></div>}</section>
    <footer>Victory AI · multi-agent business intelligence · evidence-aware discovery</footer>
    <style jsx>{` :global(*){box-sizing:border-box}:global(body){margin:0;background:#07101b;color:#eaf2f8;font-family:Inter,system-ui,sans-serif}.shell{max-width:1200px;margin:auto;padding:24px}.topbar{display:flex;justify-content:space-between;align-items:center;padding:8px 0 25px;border-bottom:1px solid #1b2a39}.topActions{display:flex;align-items:center;gap:18px}.brand{font-size:20px;font-weight:700}.brand b,.hero h1 span{color:#7ee7c4}.logo{display:inline-grid;place-items:center;width:34px;height:34px;border-radius:10px;background:#123c3a;color:#7ee7c4;font-size:11px;margin-right:9px}.badge,.live,.eyebrow{font-size:10px;letter-spacing:.14em;color:#8fa3b5}.eyebrow{color:#6dd8b5;font-weight:800;margin:0 0 10px}.hero{display:flex;justify-content:space-between;gap:30px;align-items:end;padding:60px 0 35px}.hero h1{font-size:clamp(42px,7vw,76px);line-height:.98;letter-spacing:-.05em;margin:0}.subtitle{font-size:17px;color:#9db0c0;max-width:620px;line-height:1.6}.heroCard{min-width:230px;padding:22px;border:1px solid #243646;border-radius:18px;background:#0c1824;display:flex;flex-direction:column;gap:7px}.heroCard span{color:#8da2b3;font-size:12px}.agents{margin-bottom:28px}.sectionTitle h2{margin:0 0 15px}.agentGrid{display:grid;grid-template-columns:repeat(4,1fr);gap:10px}.agentCard{background:#0b1723;border:1px solid #203243;border-radius:14px;padding:15px;text-align:left;color:#eaf2f8;cursor:pointer}.agentCard.selected{border-color:#58b89d;background:#10231f}.agentName{display:flex;justify-content:space-between;gap:8px}.agentName span{font-size:9px;text-transform:uppercase;color:#7ee7c4}.agentCard p{color:#8da1b2;font-size:12px;line-height:1.45;min-height:52px}.agentCard small{color:#6f8799;font-size:9px}.panel,.lead,.empty{background:#0b1723;border:1px solid #203243;border-radius:18px}.panel{padding:24px}.panelTitle,.resultsHead{display:flex;justify-content:space-between;gap:20px;align-items:center}.panel h2,.results h2{margin:0;font-size:23px}.panelTitle p{color:#8195a7;margin:5px 0 20px}.live{color:#7ee7c4}.live i{display:inline-block;width:7px;height:7px;border-radius:50%;background:#7ee7c4;margin-right:6px}.form{display:grid;grid-template-columns:2fr 1.4fr 1.2fr .6fr auto;gap:10px;align-items:end}label{font-size:10px;color:#8fa3b5;display:flex;flex-direction:column;gap:7px}input,select{height:45px;border:1px solid #2a3d4f;border-radius:9px;background:#07111c;color:#eef5fa;padding:0 12px;font-size:14px;outline:none}button{font:inherit}form>button{height:45px;border:0;border-radius:9px;background:#7ee7c4;color:#06110f;font-weight:800;padding:0 18px;cursor:pointer}form>button:disabled{opacity:.6}.success,.error,.warning{margin-top:14px;padding:13px;border-radius:10px;display:flex;gap:8px;flex-direction:column}.success{background:#10231f;border:1px solid #315748;color:#a8dfcb}.error{background:#2a161a;border:1px solid #71343d;color:#ffb8bd}.warning{background:#17251f;border:1px solid #315748;color:#a8dfcb}.results{padding-top:50px}.resultsHead{margin-bottom:18px}.stats{display:flex;gap:20px;color:#70879a;font-size:9px;letter-spacing:.1em}.stats b{display:block;color:#eaf2f8;font-size:21px;letter-spacing:0}.empty{min-height:270px;display:grid;place-items:center;text-align:center;padding:45px}.emptyIcon{font-size:45px;color:#557084}.empty h3{margin:0}.empty p{color:#8095a7;max-width:500px}.grid{display:grid;grid-template-columns:repeat(2,1fr);gap:12px}.lead{padding:17px}.leadTop{display:flex;align-items:center;gap:12px}.avatar{width:42px;height:42px;border-radius:12px;background:#17323b;color:#7ee7c4;display:grid;place-items:center;font-weight:800}.lead h3{margin:0;font-size:16px}.lead p{margin:3px 0 0;color:#7f94a5;font-size:12px}.score{margin-left:auto;color:#7ee7c4;font-size:18px}.meta{display:flex;gap:12px;flex-wrap:wrap;margin:17px 0 11px;color:#879bac;font-size:11px}.lead a{color:#7ee7c4;text-decoration:none;font-size:12px}footer{text-align:center;color:#5f7486;font-size:10px;margin:45px 0 10px}@media(max-width:1000px){.agentGrid{grid-template-columns:repeat(2,1fr)}.form{grid-template-columns:1fr 1fr}.form>button{grid-column:1/-1}}@media(max-width:700px){.hero{display:block;padding-top:40px}.heroCard{margin-top:22px}.grid{grid-template-columns:1fr}.topbar{align-items:flex-start}.badge{display:none}}@media(max-width:520px){.shell{padding:16px}.topbar{flex-direction:column;align-items:stretch}.topActions{align-items:stretch}.agentGrid{grid-template-columns:1fr}.form{grid-template-columns:1fr}.panel{padding:16px}.stats{display:none}} `}</style></main>;
}
